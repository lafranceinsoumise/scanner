language: python
python:
  - "3.6"
addons:
  apt:
    packages:
      - libsystemd-dev
dist: xenial
sudo: required
cache:
  - pip
  - npm

before_install:
  - pip install -U pipenv
  - cd back

install:
  - pipenv install --dev --deploy

script:
  - black --check .
  - python manage.py test -v 2
